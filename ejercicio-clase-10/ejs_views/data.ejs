<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Form</title>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Thumbnail</th>
                <th>Title</th>
                <th>Author</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            <% for (let product of products) { %>
                <tr>
                    <td><img src="<%= '../uploads/' + product.thumbnail %>" alt="<%= product.title %>"/></td>
                    <td><%= product.title %></td>
                    <td><%= product.author %></td>
                    <td><%= product.price %></td>
                </tr>
            <% } %>
        </tbody>
    </table>
  </body>
</html>